apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: kubeshort
  labels:
    app: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/ahmednasser04/kubeshort-frontend:latest
        ports:
        - containerPort: 80